# Silver Tier AI Employee Configuration
# Copy this file to .env and fill in your values
#
# SETUP INSTRUCTIONS:
#
# 1. GMAIL CONFIGURATION:
#    - Enable Gmail API in Google Cloud Console
#    - Create OAuth2 credentials (Client ID and Secret)
#    - Generate refresh token using OAuth2 flow
#    - Create Gmail App Password: Google Account > Security > App passwords
#    - Use your actual Gmail address for GMAIL_USER
#
# 2. WHATSAPP CONFIGURATION:
#    - The session path is automatically created by whatsapp_watcher.py
#    - Keywords are comma-separated terms to monitor
#    - Session data will be stored in whatsapp_session/ folder
#
# 3. LINKEDIN CONFIGURATION:
#    - Use your actual LinkedIn email and password
#    - Max posts per day limits automated posting
#    - First run requires manual login and session saving
#
# 4. PATHS:
#    - VAULT_PATH: Root directory for your AI Employee system
#    - LOGS_PATH: Directory for storing log files
#
# 5. SCHEDULER SETTINGS:
#    - Intervals control how often each task runs
#    - Shorter intervals mean more frequent checks but higher resource usage
#
# 6. APPROVAL SETTINGS:
#    - Expiry controls how long approval requests remain valid
#    - Trusted contacts can bypass approval for certain actions
#
# 7. SECURITY:
#    - DRY_RUN: Set to true to test without performing actual actions
#    - MAX_EMAILS_PER_DAY: Rate limiting for email sending
#    - REQUIRE_APPROVAL_NEW_CONTACTS: Controls approval for new contacts
#
# 8. MCP SERVER PORTS:
#    - Port numbers for Model Context Protocol servers
#    - Ensure ports are available and not used by other services
#
# 9. LOGGING:
#    - LOG_LEVEL: Controls verbosity of logs (DEBUG, INFO, WARNING, ERROR)

# Gmail Configuration
GMAIL_CLIENT_ID=your_client_id
GMAIL_CLIENT_SECRET=your_client_secret
GMAIL_REFRESH_TOKEN=your_refresh_token
GMAIL_USER=your_email@gmail.com
GMAIL_APP_PASSWORD=your_16_char_app_password

# WhatsApp Configuration  
WHATSAPP_SESSION_PATH=./whatsapp_session
WHATSAPP_KEYWORDS=urgent,asap,invoice,payment,help,pricing

# LinkedIn Configuration
LINKEDIN_EMAIL=your_linkedin_email
LINKEDIN_PASSWORD=your_linkedin_password
LINKEDIN_MAX_POSTS_PER_DAY=3

# Paths
VAULT_PATH=./
LOGS_PATH=./Logs

# Scheduler Settings
CHECK_INTERVAL_MINUTES=2
DASHBOARD_UPDATE_MINUTES=5
HEALTH_CHECK_MINUTES=30

# Approval Settings
APPROVAL_EXPIRY_HOURS=24
AUTO_APPROVE_TRUSTED_CONTACTS=true

# Security
DRY_RUN=false  # Set to true for testing without real actions
MAX_EMAILS_PER_DAY=50
REQUIRE_APPROVAL_NEW_CONTACTS=true

# MCP Server Ports
EMAIL_MCP_PORT=3000

# Logging
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR